LOADING.KWI
```
┌──────────────────────┬─────────────────────────────────────┐
│                      │                                     │
│   KWI-заголовок      │        ext2 файловая система        │
│   (~1.1 МБ)          │        (~335 МБ)                    │
│                      │                                     │
│  • magic: 0x0001     │  Обычный Linux ext2:                │
│  • ID: "HL57"        │  ├── bin/         (busybox)         │
│  • ID: "VL57"        │  ├── sbin/init → ../vns/bin/pmng    │
│  • карта блоков      │  ├── vns/bin/*.so (вся логика)      │
│  • дерево каталогов  │  ├── etc/                           │
│  • список файлов     │  └── ...                            │
│                      │                                     │
└──────────────────────┴─────────────────────────────────────┘
```
LOADING.KWI Это ext2 (та же файловая система что в Linux). Windows-инструмент 7zip видит ZIP потому что где-то внутри ext2 лежит PK байт (случайное совпадение в данных), и виндовый распаковщик цепляется за него.

Как работает kwi.sh:
- unpack: отрезает заголовок → получает чистый ext2 → debugfs rdump выгружает все файлы
- pack: mkfs.ext2 -d создаёт новый ext2 из файлов → приклеивает заголовок обратно

```
#   ./kwizipper.sh unpack <kwi> [dir]
#   ./kwizipper.sh pack [dir] <kwi_out>
```
Вот и всё. Никакой магии.
